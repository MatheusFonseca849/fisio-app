import { styled } from "styled-components";

const StyledJointHealthSheet = styled.form`
  display: flex;
  flex-direction: column;
  gap: var(--gap-default);
  background-color: transparent;
  width: fit-content;
  max-width: 90%;
  padding: 16px 0;
`;

export default StyledJointHealthSheet;
